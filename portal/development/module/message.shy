title "数据消息"
brief `无论是前后端通信、模块间通信、设备间通信，都使用统一的通信协议。`
spark `如下所示，后端接口的第一个参数为 <code>m *ice.Message</code>，保存了数据消息，并封装了相关的接口。`
spark inner `
func (s hi) List(m *ice.Message, arg ...string) {
}
` file demo.go
spark `如下所示，前端接口的第二个参数为 <code>msg</code>，保存了数据消息，并封装了相关的接口。`
spark inner `
_init: function(can, msg) {
}
` file demo.js
chapter "基础协议"

section "msg.Option()"